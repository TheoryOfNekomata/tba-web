/*global $*/

(function($, undefined) {

    "use strict";

    $(function() {
        $("[data-spy='scroll--custom']").each(function(i, scrollspy) {
            var scrollspyNav = $(scrollspy).data("target");


        });
    });

})($);
